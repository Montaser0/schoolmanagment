# PRD - School Manager

## 1) نظرة عامة على المنتج

`School Manager` هو نظام لإدارة المدارس يعمل على بنية متعددة المستأجرين (Multi-tenant)، ويغطي:
- إدارة بيانات المدرسة والموظفين والطلاب.
- إدارة الحضور اليومي للطلاب والموظفين.
- إدارة الأقساط والدفعات (طلاب ورواتب موظفين).
- إدارة الإيرادات والمصروفات مع ملخصات مالية وتصدير Excel.

المنصة مبنية على `Next.js + Supabase` مع اعتماد `Server Actions` و`RLS` لعزل بيانات كل مدرسة.

## 2) أهداف المنتج

- رقمنة العمليات الإدارية والمالية في المدرسة.
- توفير لوحة تشغيل يومية للموظفين.
- تقليل الأخطاء اليدوية في الحضور والدفعات والسجلات المالية.
- تمكين صاحب النظام/المالك من إدارة المدارس والاشتراكات.

## 3) نطاق المشروع (In Scope)

- المصادقة وتسجيل الدخول.
- لوحات تحكم حسب الدور (Admin / Staff).
- CRUD للصفوف والطلاب والموظفين.
- حضور يومي ونسب حضور شهرية.
- أقساط الطلاب ورواتب الموظفين.
- دفتر إيرادات ومصروفات.
- تصدير البيانات المالية إلى Excel.

## 4) الأدوار (Roles)

- `owner`:
  - مالك النظام/المدرسة.
  - يصل إلى مسارات الإدارة (`/admin`).
- `staff`:
  - المستخدم التشغيلي اليومي.
  - يصل إلى مسارات التشغيل (`/staff`).

ملاحظة: يوجد تطبيع في الكود بحيث `admin -> owner` و`teacher/employee -> staff`.

## 5) مصفوفة الصلاحيات (Permissions Matrix)

### owner
- الوصول إلى:
  - `/admin`
  - `/admin/schools`
- إدارة إنشاء مدرسة جديدة + حساب مالك + خطة اشتراك.
- لا يستخدم صفحات التشغيل اليومية الخاصة بالـ`staff`.

### staff
- الوصول إلى:
  - `/staff`
  - `/staff/class`
  - `/staff/students`
  - `/staff/studentlist`
  - `/staff/student-installments`
  - `/staff/addteachers`
  - `/staff/teacherslist`
  - `/staff/teacher-installments`
  - `/staff/revenues`
  - `/staff/expenses`
- إجراء عمليات CRUD التشغيلية (طلاب/موظفين/صفوف).
- تسجيل الحضور.
- إدارة الأقساط والدفعات.
- إدارة السجل المالي والتصدير.

### ضوابط الحماية
- المستخدم غير المسجل يُعاد توجيهه إلى تسجيل الدخول.
- في لوحة `admin`: مسموح فقط لـ`owner`.
- في لوحة `staff`: المستخدم `owner` يُعاد توجيهه إلى `admin`.
- عزل بيانات كل مدرسة يعتمد على `school_id` + سياسات `RLS`.

## 6) هيكل الصفحات (Pages & Routes)

## الصفحات العامة والمصادقة
- `/`
  - نقطة الدخول.
  - تعيد التوجيه حسب حالة التسجيل والدور.
- `/auth/login`
  - تسجيل الدخول.
- `/auth/error`
  - عرض أخطاء المصادقة.
- `/auth/sign-up-success`
  - رسالة نجاح التسجيل/التفعيل.

## صفحات الإدارة (Admin)
- `/admin`
  - لوحة إدارة عليا (حاليًا بسيطة).
- `/admin/schools`
  - إنشاء مدرسة.
  - إنشاء مالك المدرسة.
  - اختيار خطة الاشتراك: `trial`, `basic`, `pro`, `enterprise`.

## صفحات التشغيل (Staff)
- `/staff`
  - Dashboard تشغيلي:
    - إحصائيات رئيسية.
    - حضور أسبوعي.
    - توزيع الجنس.
    - مالية شهرية.
- `/staff/class`
  - إدارة الصفوف (إضافة/تعديل/حذف).
- `/staff/students`
  - إضافة طالب جديد.
- `/staff/studentlist`
  - قائمة الطلاب.
  - بحث/فلترة/ترقيم صفحات.
  - تحديث حضور يومي.
  - تعديل/حذف بيانات.
  - نسب حضور شهرية.
- `/staff/student-installments`
  - إدارة أقساط الطلاب.
  - تسجيل دفعات.
  - حالات القسط (مثل unpaid/partial/paid/late).
- `/staff/addteachers`
  - إضافة معلم/موظف.
- `/staff/teacherslist`
  - قائمة الموظفين.
  - بحث/فلترة/ترقيم صفحات.
  - تحديث حضور يومي.
  - تعديل/حذف بيانات.
  - نسب حضور شهرية.
- `/staff/teacher-installments`
  - إدارة أقساط/رواتب الموظفين.
  - تسجيل الدفعات.
- `/staff/revenues`
  - الإيرادات اليدوية + دفعات الطلاب.
  - ملخص مالي.
  - تصدير Excel.
- `/staff/expenses`
  - المصروفات اليدوية + دفعات رواتب الموظفين.
  - ملخص مالي.
  - تصدير Excel.

## واجهات API
- `/api/exports/revenues`
  - تصدير الإيرادات إلى Excel.
- `/api/exports/expenses`
  - تصدير المصروفات إلى Excel.

## 7) المميزات الرئيسية (Features)

## A) المصادقة وإدارة الجلسة
- تسجيل الدخول باستخدام البريد وكلمة المرور.
- تسجيل الخروج.
- توجيه تلقائي حسب الدور.

## B) إدارة المدارس والاشتراكات
- إنشاء مدرسة جديدة.
- ربط المدرسة بحساب مالك.
- حفظ خطة الاشتراك وتواريخ بدايتها ونهايتها.

## C) إدارة البيانات الأكاديمية
- إدارة الصفوف.
- إدارة الطلاب.
- إدارة الموظفين/المعلمين.
- CRUD كامل مع عزل بيانات المدرسة.

## D) الحضور
- تسجيل حضور يومي للطلاب.
- تسجيل حضور يومي للموظفين.
- حساب نسب حضور شهرية.
- التعامل مع الأيام السابقة غير المسجلة كغياب افتراضي عند الحاجة.

## E) الأقساط والدفعات
- تعريف قسط لكل طالب.
- تسجيل دفعات متعددة على القسط.
- تعريف قسط/راتب لكل موظف.
- تسجيل دفعات الرواتب.

## F) المالية والتقارير
- دفتر إيرادات موحد (يدوي + دفعات أقساط الطلاب).
- دفتر مصروفات موحد (يدوي + دفعات رواتب الموظفين).
- ملخص صافي (إيرادات - مصروفات).
- تصدير Excel للإيرادات والمصروفات.

## 8) متطلبات غير وظيفية (Non-functional)

- أمان وصول عبر جلسات مصادقة Supabase.
- عزل متعدد المدارس عبر `school_id` وسياسات `RLS`.
- أداء مناسب لعمليات القوائم مع الفلترة والترقيم.
- قابلية تتبع مالية عبر سجلات الدفعات.

## 9) قواعد البيانات والكيانات الأساسية (High-level)

- `users`: بيانات المستخدمين والأدوار.
- `schools`: بيانات المدارس والخطط.
- `classrooms`: الصفوف.
- `students`: الطلاب.
- `teachers`: الموظفون/المعلمون.
- `student_attendance`: حضور الطلاب.
- `teacher_attendance`: حضور الموظفين.
- `student_installments` + `student_installment_payments`.
- `teacher_installments` + `teacher_installment_payments`.
- `revenues` و`expenses`.

## 10) تدفقات الاستخدام الأساسية (User Flows)

## Flow 1: تسجيل الدخول
- المستخدم يفتح `/auth/login`.
- إدخال البريد وكلمة المرور.
- النظام يتحقق من الجلسة.
- إعادة توجيه حسب الدور:
  - `owner -> /admin`
  - `staff -> /staff`

## Flow 2: تشغيل يوم دراسي
- الموظف يدخل `studentlist` و`teacherslist`.
- يسجل حضور اليوم.
- يحدّث أي بيانات ناقصة للطلاب/الموظفين.
- يراجع ملخص `staff dashboard`.

## Flow 3: إدارة مالية شهرية
- تسجيل دفعات أقساط الطلاب.
- تسجيل دفعات رواتب الموظفين.
- إضافة أي إيرادات/مصروفات يدوية.
- مراجعة الصافي.
- تصدير تقرير Excel.

## 11) مؤشرات نجاح مقترحة (KPIs)

- نسبة اكتمال تسجيل الحضور اليومي.
- زمن إنجاز العمليات الإدارية (إضافة طالب/موظف/قسط).
- دقة السجل المالي (انخفاض الأخطاء اليدوية).
- عدد عمليات التصدير الشهرية للتقارير.

## 12) ملاحظات تقنية ومخاطر حالية

- يوجد تعارض محتمل في إنشاء مالك المدرسة: في بعض المسارات يتم حفظ دوره كـ`staff` بدل `owner`، وقد يؤثر على وصوله إلى `/admin`.
- لوحة `/admin` الحالية بسيطة وتحتاج توسعة لتغطي إدارة عليا كاملة.
- الصلاحيات داخل `staff` ليست مفصلة جدًا (كل staff يملك صلاحيات تشغيل واسعة حاليًا).

## 13) تحسينات مستقبلية (Roadmap)

- تفصيل صلاحيات staff إلى مستويات (Manager, Accountant, Registrar).
- إضافة تقارير مرئية متقدمة (رسوم بيانية شهرية/سنوية).
- نظام تنبيهات للأقساط المتأخرة.
- سجل تدقيق (Audit Log) لكل عملية حساسة.
- دعم أرشفة سنوية وتصدير شامل.
